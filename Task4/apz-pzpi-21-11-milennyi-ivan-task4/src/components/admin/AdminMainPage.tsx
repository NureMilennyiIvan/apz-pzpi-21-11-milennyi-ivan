import { useTranslation } from "react-i18next";
import { IUserProps } from "../properties/IUserProps";

export const AdminMainPage: React.FC<IUserProps> = ({user}) =>{
    const {t} = useTranslation();
    return (<></>)
}